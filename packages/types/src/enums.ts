/**
 * Shared Enums
 */

export enum SupportedDoc {
  NEXTJS = "nextjs",
  REACT = "react",
  TYPESCRIPT = "typescript",
}

export enum ResponseMode {
  AUTO = 'auto',
  FRONTEND = "frontend",
  BACKEND = "backend",
  FULLSTACK = "fullstack",
  DEBUG = "debug",
  ARCHITECTURE = "architecture",
}
