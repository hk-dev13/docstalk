# DocsTalk CLI - Environment Variables

# ============================================================================
# AUTHENTICATION (Required for dev commands)
# ============================================================================

# Admin token for developer commands
# Generate: node -e "console.log('dtalk_admin_' + require('crypto').randomBytes(32).toString('hex'))"
# DOCSTALK_ADMIN_TOKEN=dtalk_admin_YOUR_SECRET_KEY_HERE

# Remote token (for access outside project directory)
# Must match DOCSTALK_ADMIN_TOKEN
# DOCSTALK_REMOTE_TOKEN=dtalk_admin_YOUR_SECRET_KEY_HERE

# ============================================================================
# API CONFIGURATION
# ============================================================================

# DocsTalk API URL
# Default: http://localhost:3001
# DOCSTALK_API_URL=http://localhost:3001

# ============================================================================
# DEVELOPMENT
# ============================================================================

# Force development mode
# Default: auto-detected based on pnpm-workspace.yaml
# DOCSTALK_DEV=1

# ============================================================================
# NOTES
# ============================================================================

# Security Best Practices:
# 1. Never commit this file with actual tokens
# 2. Use strong, random tokens (min 64 chars)
# 3. Rotate tokens every 90 days
# 4. Store in secrets manager for production
# 5. Use different tokens per environment

# Quick Setup:
# 1. Copy this file: cp .env.example .env
# 2. Generate token: node -e "console.log('dtalk_admin_' + require('crypto').randomBytes(32).toString('hex'))"
# 3. Set DOCSTALK_ADMIN_TOKEN in .env
# 4. Run: docstalk dev serve
